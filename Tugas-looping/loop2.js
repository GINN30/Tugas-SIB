//nilai awal
let startValue = 0;

//perulangan looping
for (let i = 1; i <= 10; i++) {
    //set nilai awal selalu di tambah 2 setiap perulangan
    startValue += 2;
    //menampilkan hasil
    console.log(`Hasil penambahan pada pengulangan ke-${i} adalah ${startValue}`);
}